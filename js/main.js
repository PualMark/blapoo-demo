// Start on #home    var url = document.URL,       index = url.indexOf("#"),      hash = index != -1 ? url.substring(index+1) : "";    if(hash === ""){        location.hash = "#home";    }//Smooth Scroll  $("a").on("click", function (e) {    // 1    e.preventDefault();    // 2    const href = $(this).attr("href");    // 3    $("html, body").animate({ scrollTop: $(href).offset().top }, 600);  });// Toggle Button Function to Show Navigation Bar Mobile  function showNavbar() {    var checkBox = document.getElementById('show-nav');    var content = document.getElementById('navbar');    var line1 = document.getElementById('toggle-line1');    var line2 = document.getElementById('toggle-line2');    if(checkBox.checked == true){      content.style.display = 'block'      content.style.animation = 'nav-display .4s'      //lines      line1.style.animation = 'toggle-line-1 .4s forwards'      line2.style.animation = 'toggle-line-2 .4s forwards'    }else{      content.style.animation = 'nav-nodisplay .4s forwards'      line1.style.animation = 'toggle-line-out-1 .4s forwards'      line2.style.animation = 'toggle-line-out-2 .4s forwards'    }  }//  function scrollAppear(){    var thingToAppear = document.querySelector('.section');    var thingPosition = thingToAppear.getBoundingClientRect().top;    var screenPosition = window.innerHeight / 1.3;    if(thingPosition < screenPosition){        thingToAppear.classList.add('appear');    }  }  window.addEventListener('scroll', scrollAppear);//CARD  // DOM Element selections  const cardWrapper = document.querySelector(".ux-image");  const card = document.querySelector(".tab-container");  const highlight = document.querySelector(".highlight");  // highest values for angle  const mostX = 10; // 10 or -10  const mostY = 10; // 10 or -10  cardWrapper.addEventListener("mousemove", (e) => {    // remove transition    card.style.transition = "none";    highlight.style.transition = "none";    const x = e.offsetX;    const y = e.offsetY;    const { width, height } = cardWrapper.getBoundingClientRect();    const halfWidth = width / 2;    const halfHeight = height / 2;    // calculate angle    const rotationY = ((x - halfWidth) / halfWidth) * mostX;    const rotationX = ((y - halfHeight) / halfHeight) * mostY;    // set rotation    card.style.transform = `rotateY(${rotationY}deg) rotateX(${rotationX}deg)`;    highlight.style.left = `${(rotationY / mostX) * 30 * -1}%`;    highlight.style.top = `${(rotationX / mostY) * 30 * -1}%`;  });  cardWrapper.addEventListener("mouseleave", () => {    // add transition back    card.style.transition = "transform 0.5s ease-in-out";    card.style.transform = `rotateY(0) rotateX(0)`;    highlight.style.transition = "left 0.5s ease-in-out, top 0.5s ease-in-out";    // add default position back to highlight    highlight.style.left = `-20%`;    highlight.style.top = `-13%`;  });//ABOUT DETECTION